# Node Naming Conventions

Use names like `clean_01_generator`, `clean_02_approval`, `clean_03_renderer`, `clean_04_publisher`. Prefix debug-only nodes with `debug_` and approval nodes with `approval_`.

